import { INavButtonsID } from '@assos/lib/commonTypes';

export const navButtonsID: INavButtonsID = {
  search: 'header-search-btn',
  backToSearch: 'header-back-to-search-btn',
  share: 'header-share-btn'
};

export function updateCartCountBadge(count?: number): void {
  // TODO: Navigation - update badge number
}

export function registerEventHandlers(): void {
  // TODO: Navigation - global buttons
}
