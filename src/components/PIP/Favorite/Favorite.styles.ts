import { StyleSheet} from 'react-native';

const styles = StyleSheet.create({
  container: {
    position: 'absolute',
    right: 10,
    top: 10
  },
  star: {
    height : 19,
    width: 19
  }
});

export default styles;
